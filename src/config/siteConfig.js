// src/config/siteConfig.js

const siteConfig = {
  studioName: "Your Roblox Studio",
  // add other global config fields as needed
  // e.g. heroTagline: "...", social links, etc.
};

export default siteConfig;
export { siteConfig };
